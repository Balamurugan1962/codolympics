# Codolympics web app

The UI and API for both phases, in one Next.js process. Phase 1 is the
qualifying round (logical puzzles, then hacking); Phase 2 is the auction coding
contest. The judge in [`../judge`](../judge) is the only thing that runs code.

- **What it must do:** [`../docs/requirements-phase1.md`](../docs/requirements-phase1.md),
  [`../docs/requirements-backend.md`](../docs/requirements-backend.md),
  [`../docs/requirements-frontend.md`](../docs/requirements-frontend.md)
- **How it is built:** [`../docs/lld.md`](../docs/lld.md)
- **Why:** [`../docs/decisions.md`](../docs/decisions.md)

## Run it (development)

```bash
docker compose up -d                 # Postgres
cp .env.example .env                 # fill in the secrets
pnpm install
pnpm db:migrate
pnpm dev                             # http://localhost:3000
```

The judge must be reachable at `JUDGE_URL` with the same `JUDGE_SERVICE_TOKEN`;
see [`../judge/README.md`](../judge/README.md). `PROBLEMS_DIR` must be the
directory the judge reads problems from — the app writes there, the judge
reads there.

The first administrator is created on first start from `ADMIN_USERNAME` /
`ADMIN_PASSWORD`. There is no default password: unset means no admin.

**`BETTER_AUTH_URL` must be exactly the address participants type in**, scheme
and port included. Sign-in is refused from any other origin. On the hall LAN
that is the server's IP, not `localhost`.

## Run it (the whole contest)

The root [`../docker-compose.yml`](../docker-compose.yml) brings up all four
services — Postgres, the sandbox, the judge and this app — with one command.
Only the app publishes a port.

## Tests

```bash
pnpm test                                        # 19 tests; needs the docker Postgres
BASE_URL=http://localhost:3000 pnpm exec tsx tests/e2e/smoke.ts   # 37 steps, both phases, real judge
```

The auction test uses a separate `contest_test` database (it truncates tables).
The smoke test needs a fresh contest in the `registration` phase and a running
judge; it is the check to run on the contest machine before the contest.

## Layout

```
src/
  db/schema.ts           every contest table, from docs/lld.md §3
  db/auth-schema.ts      Better Auth's tables (generated by `pnpm auth:schema`)
  lib/                   the engine -- one job per file
    auth.ts              Better Auth: roles, single session for participants
    registration.ts      self-registration, staff accounts, first admin
    auction.ts           bids, awards, the restart-safe clock
    submissions.ts       submit, poll the judge, cancel, rejudge
    hints.ts             buy the next hint
    scoring.ts           standings, derived on read
    phases.ts            the phase machine and its blockers
    phase1-puzzles.ts    Section A answering and scoring
    phase1-hacking.ts    Section B attempts
    phase1-review.ts     grading queue, Phase 1 standings, advancement
    phase1-admin.ts      authoring, self-tests, publishing
    problems.ts          package upload -> validate -> publish (symlink swap)
    admin.ts             overrides: void, refund, adjust, announce, export
    scheduler.ts         the one background loop
    events.ts            in-process SSE bus
    judge.ts             the only file that talks to the judge
    api.ts               ApiError and the route wrapper
  app/api/               route handlers: check, call the engine, respond
  app/(app)/             signed-in pages; layout loads state and opens SSE
  app/(auth)/            sign-in and registration
  components/            shell (participants and evaluators), local-time (a
                         timestamp the server must not format), admin/ (the
                         console sidebar, the problem, puzzle and hacking
                         builders), phase1/ and problems/ (question views
                         shared by participants and admin previews),
                         editor (Monaco, bundled), SSE provider, countdown,
                         markdown, logo, icons, the ReasonAction dialog
  components/ui/         page (body, header, section, toolbar), wizard,
                         button, card, input, form, choice, markdown-editor,
                         file-drop, list-editor, summary, badge, alert,
                         table, tabs, dialog, drawer, menu, toast, skeleton,
                         empty-state, stat, stepper, countdown-ring,
                         split-pane, kbd -- all hand-rolled
tests/                   scoring, leak contracts, concurrent bidding, e2e
```

## Things worth knowing

**Every deadline is a database column**, read by `scheduler.ts` once a second.
A restart loses nothing. Clients render countdowns against the server clock via
an offset measured on every SSE event.

**Every participant-facing response goes through a view function** —
`participantView`, `participantQuestion`, `participantHackQuestion`,
`participantAttempt` — and `tests/leaks.test.ts` asserts none of them carry
`jury_detail`, an answer key, a validator, a model answer or a hack verdict.

**Every administrator mutation requires a reason** and writes the audit log in
the same transaction as the change. That includes the two irreversible ones in
Settings → Danger zone: *Reset the contest* removes every participant and
everything they did and returns to registration, keeping problems, Phase 1
questions and settings; *Wipe everything* also deletes every package on the
judge, every question and the settings. Both need their phrase typed, and the
server checks it again. The audit log itself is never truncated.

**Announcements have their own page** (`/admin/announcements`): Markdown with a
preview, a Send button, and the history. They reach every signed-in screen at
once over SSE and stay on every participant's home page.

**Money is only ever moved in `auction.ts`, `hints.ts` and `admin.ts`**, always
under `FOR UPDATE`, always with a ledger row. The lock order is lot → participant
and nothing else ever locks in a different order.

**A timestamp from server-rendered state goes through `<LocalTime>`.** The
server is UTC and the hall is not, so formatting one during render makes the
markup disagree with the browser and React discards it. Data fetched in the
browser has no such problem and formats inline.

**Monaco is bundled**, not loaded from a CDN. Fonts are the system stack. The
page loads nothing from outside the contest server.

**The UI follows a few rules everywhere.** One primary action per screen; the
phase stepper always visible; anything an administrator creates (a problem, a
puzzle, a hacking question) is a stepped form that ends in a preview of exactly
what participants will see, and nothing goes live without its self-test; a toast for every outcome (bid accepted, outbid,
verdict, announcement); skeletons while loading and empty states that say what
to do next; `Ctrl+Enter` submits code, `←`/`→` move between puzzles, `J`/`K`
move through the grading queue; two-pane layouts collapse to tabs below 1024px.

**Login is rate-limited per IP** in production (10 sign-ins per 10 s). A script
signing in many users from one machine must pace itself; people never notice.
