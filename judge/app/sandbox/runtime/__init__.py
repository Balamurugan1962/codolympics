"""Scripts copied into the sandbox and executed there.

Nothing in here may import from the judge: inside the sandbox only the
standard library and these three files exist. checker_runtime.py is always
copied in, because the other two reuse its Reader and module loader.
"""
