"""go-judge, and everything that runs inside it.

`client` talks to the sandbox, `command` builds what it is asked to run,
`scripts` runs the problem setters' Python inside it, and `runtime/` holds the
files that are copied in to make that possible.
"""
